version: "3.8"
services:
  mailer:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3000:3000" # bind to localhost only; nginx will reverse proxy
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
